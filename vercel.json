{
  "buildCommand": "yarn install --ignore-engines && yarn build",
  "installCommand": "yarn install --ignore-engines",
  "build": {
    "env": {
      "NODE_OPTIONS": "--openssl-legacy-provider"
    }
  },
  "rewrites": [
    {
      "source": "/api/:match*",
      "destination": "netease-cloud-music-api-two-gamma-50.vercel.app/:match*"
    }
  ]
}
